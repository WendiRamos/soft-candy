@model IEnumerable<SoftCandy.Models.Vendedor>

@{
    ViewData["Title"] = "Vendedor";
}
<h2>Vendedores</h2>

<form class="form-inline" asp-controller="Busca" asp-action="BuscaVendedor">
    <div class="form-group">
        <div class="form-check-inline">
            <label for="Nome"></label>
            <input type="text" class="form-control" name="Nome" placeholder="Insira um nome" value=@ViewData["NomeCliente"] />
        </div>
        <button type="submit" class=" btn btn-primary">Pesquisar</button>
    </div>
    <br />
</form>

<div>
    <a asp-action="Create" class="btn btn-success">Novo Vendedor</a> |
    <a asp-action="Relatorio" target="_blank" class="btn btn-info">Gerar Relatório</a>
</div>
<br />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdVendedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomeVendedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CelularVendedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnderecoVendedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailVendedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdVendedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeVendedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CelularVendedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnderecoVendedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailVendedor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVendedor">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdVendedor">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVendedor">Deletar</a>
            </td>
        </tr>
        }
    </tbody>
</table>
