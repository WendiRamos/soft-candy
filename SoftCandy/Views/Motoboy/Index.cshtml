@model IEnumerable<SoftCandy.Models.Motoboy>

@{
    ViewData["Title"] = "Index";
}
<form class="form-inline" asp-controller="Busca" asp-action="BuscaMotoboy">
    <div class="d-flex justify-content-between">
        <div class="d-flex">
            <a asp-action="Create" class="btn btn-success mr-1">Novo Motoboy</a>
            <a asp-controller="Busca" asp-action="BuscaMotoboyApagado" class="btn btn-info">Motoboy Apagados</a>
        </div>
        <div class="d-flex">
            <label for="Nome"></label>
            <input type="text" class="form-control mr-1" name="Nome" placeholder="Insira um nome" value=@ViewData["Nome"] />
            <button type="submit" class=" btn btn-primary">Pesquisar</button>
        </div>
    </div>
</form>
<br />
<center>
    <div class="card border-primary mb-3">
        <div class="card-header"><h4>Motoboys</h4></div>
        <div class="card-body">
            @if (Model.Count() != 0)
            {
                <table class="table">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Nome)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Celular)
                            </th>
                            <th class="center">
                                Visualizar
                            </th>
                            <th class="center">
                                Editar
                            </th>
                            <th class="center">
                                Apagar
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Id)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Nome)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Celular)
                                </td>
                                <td class="center">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="sem-sub">
                                        <img src="~/icons/detalhes.svg" class="icone-menus" title="Visualizar" />
                                    </a>
                                </td>
                                <td class="center">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="sem-sub">
                                        <img src="~/icons/editar.svg" class="icone-menus" title="Editar" />
                                    </a>

                                </td>
                                <td class="center">

                                    <a asp-action="Delete" asp-route-id="@item.Id" class="sem-sub">
                                        <img src="~/icons/deletar.svg" class="icone-menus" title="Apagar" />
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>Não tem motoboys para mostrar</p>
            }
        </div>
    </div>
</center>
