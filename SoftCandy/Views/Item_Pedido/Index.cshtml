@model IEnumerable<SoftCandy.Models.Item_Pedido>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Preco_Pago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Preco_Pago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Produto.Nome_Produto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Num_Pedido">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Num_Pedido">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Num_Pedido">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
