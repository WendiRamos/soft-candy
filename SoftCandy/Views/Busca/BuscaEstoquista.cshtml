<!DOCTYPE html>
@model IEnumerable<SoftCandy.Models.Estoquista>

@{
    ViewData["Title"] = "Pesquisar Estoquista";
}
<center>
    <form class="form-inline" asp-controller="Busca" asp-action="BuscaEstoquista">
        <div class="d-flex justify-content-between">
            <div class="d-flex">
                <a asp-controller="Estoquista" asp-action="Index" class="btn btn-info mr-1">Voltar</a>
            </div>
            <div class="d-flex">
                <label for="Nome"></label>
                <input type="text" class="form-control mr-1" name="Nome" placeholder="Insira um nome" value=@ViewData["Nome"] />
                <button type="submit" class=" btn btn-primary">Pesquisar</button>
            </div>
        </div>
    </form>
    <br />
    <div class="card border-primary mb-3">
        <div class="card-header"><h4>Estoquistas</h4></div>
        <div class="card-body">
            @if (Model.Count() != 0)
            {
                <table class="table ">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.IdEstoquista)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.NomeEstoquista)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.CelularEstoquista)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.EmailEstoquista)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.IdEstoquista)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NomeEstoquista)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.CelularEstoquista)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EmailEstoquista)
                                </td>
                                <td>
                                    <a asp-controller="Estoquista" asp-action="Edit" asp-route-id="@item.IdEstoquista">Editar</a> |
                                    <a asp-controller="Estoquista" asp-action="Details" asp-route-id="@item.IdEstoquista">Detalhes</a> |
                                    <a asp-controller="Estoquista" asp-action="Delete" asp-route-id="@item.IdEstoquista">Deletar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>Não tem estoquistas para mostrar</p>
            }
        </div>
    </div>
</center>