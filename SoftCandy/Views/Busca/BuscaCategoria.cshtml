<!DOCTYPE html>
@model IEnumerable<SoftCandy.Models.Categoria>

@{
    ViewData["Title"] = "Pesquisar Categoria";
}
<center>
    <form class="form-inline" asp-controller="Busca" asp-action="BuscaCategoria">
        <div class="d-flex justify-content-between">
            <div class="d-flex">
                <a asp-controller="Categoria" asp-action="Index" class="btn btn-info mr-1">Voltar</a>
            </div>
            <div class="d-flex">
                <label for="Nome"></label>
                <input type="text" class="form-control mr-1" name="Nome" placeholder="Insira um nome" value=@ViewData["Nome"] />
                <button type="submit" class=" btn btn-primary">Pesquisar</button>
            </div>
        </div>
    </form>
    <br />
    <div class="card border-primary mb-3">
        <div class="card-header"><h4>Categorias</h4></div>
        <div class="card-body">
            @if (Model.Count() != 0)
            {
                <table class="table">
                    <thead>
                        <tr class="text-center">
                            <th>
                                @Html.DisplayNameFor(model => model.IdCategoria)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.NomeCategoria)
                            </th>
                            <th>
                                Visualizar
                            </th>
                            <th>
                                Editar
                            </th>
                            <th>
                                Apagar
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr class="text-center">
                                <td>
                                    @Html.DisplayFor(modelItem => item.IdCategoria)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NomeCategoria)
                                </td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@item.IdCategoria" class="sem-sub">
                                        <img src="~/icons/detalhes.svg" class="icone-menus" title="Visualizar" />
                                    </a>
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.IdCategoria" class="sem-sub">
                                        <img src="~/icons/editar.svg" class="icone-menus" title="Editar" />
                                    </a>

                                </td>
                                <td>
                                    <a asp-action="Delete" asp-route-id="@item.IdCategoria" class="sem-sub">
                                        <img src="~/icons/deletar.svg" class="icone-menus" title="Apagar" />
                                    </a>

                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>Não tem categorias para mostrar</p>
            }
        </div>
    </div>
</center>